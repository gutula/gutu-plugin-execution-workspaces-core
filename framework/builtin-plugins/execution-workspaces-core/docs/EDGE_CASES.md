# Edge Cases

- Keep workspace state truthful when services restart or stop.
- Allow sandbox, worker, and same-process isolation postures without changing API shape.
- Provision default preview and code-runner services whenever a workspace is realized.
